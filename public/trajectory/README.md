# 轨迹文件

- truck.json：车辆；worker.json：人员；drone.json：无人机示例巡检航线。
- longitude、latitude 为经纬度（度），timestamp 为带时区的时间。
- 无人机 height 单位为米，表示相对航线采样地形最高点的高度，当前示例为 100–150 米。不是海拔，也不是实时离地高度。
- 场景加载时沿无人机航线每 5 秒采样一次地形并确定高度基准；经纬度与高度按时间插值。地形采样不能保证避开未采样的尖峰或建筑物，此航线仅用于展示。
- 航线结束后无人机在终点悬停，旋翼继续转动；暂停播放时所有动画暂停。全局回放循环后重新开始。
- 修改 JSON 后刷新页面生效；车辆、人员和无人机都会在三维实景中读取各自轨迹。
